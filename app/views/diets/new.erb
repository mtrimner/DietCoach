
<% if @diet.errors.any? %>
        <h5 class="error"><%= @diet.errors.full_messages.to_sentence %></h5>
<% end %>
<div class="d-flex align-items-center flex-column">
    <%= bootstrap_form_with model: [@user, @diet], method: 'post' do |f| %>
  
   <%= render 'form', f: f %>

<% end %>